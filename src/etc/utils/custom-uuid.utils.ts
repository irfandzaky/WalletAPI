// import { Injectable } from '@nestjs/common';
// import { v4 as uuidv4 } from 'uuid';

// export function generateCustomUUID(){
//   const randomDigits = Math.floor(1000 + Math.random() * 9000);
//   return `transaksi${randomDigits}-${uuidv4()}`;
// }